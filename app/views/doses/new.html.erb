<div class = "container mt-5">
  <div class = "mb-4">
    <h1>Add in doses for <%=@cocktail.name %></h1>
  </div>
    <%= simple_form_for([@cocktail, @dose]) do |f| %>
    <%= f.association :ingredient %>
    <%= f.input :description %>
    <%= f.submit %>
    <% end %>
</div>